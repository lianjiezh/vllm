# Checklist

本清单用于在文档生成完成后逐项验证 `docs/design/attention_backend_framework.md` 是否满足
`spec.md` 中所有 ADDED Requirements。每项已实际打开文档与源码核对。

> **文件名修正**：原计划文件名为 `docs/design/attention_backends.md`，但该名已被
> `tools/pre_commit/generate_attention_backend_docs.py` 自动生成占用
> （文件头标注 "Do not edit this file manually"）。为避免破坏 pre-commit hook，
> 实际产出文件改名为 `docs/design/attention_backend_framework.md`，并在文档开头
> 交叉引用了原 auto-generated 文档。下面所有 checkpoint 中的文件名按实际产出文件
> 核对。

## 文档存在性与基础结构

- [x] 文件 `docs/design/attention_backend_framework.md` 存在，含 SPDX 头部
  （第 1–2 行 `<!-- SPDX-License-Identifier: Apache-2.0 -->` 与
  `<!-- SPDX-FileCopyrightText: ... -->`）。
- [x] 文档开头有完整目录（TOC，第 10–50 行），与正文标题一一对应
  （概述、核心抽象层、backend 注册与发现、backend 选择机制、各平台优先级表、
  完整 backend 清单、端到端调用链、横切关注点，各小节均覆盖）。
- [x] 文档使用中文，术语与现有 `docs/design/model_runner_v2.md` 风格一致
  （如 `kv_cache_group` / `CommonAttentionMetadata` / `static_forward_context`
  等术语拼写一致）。

## 覆盖"核心抽象层" requirement

- [x] 有专门章节讲解 `AttentionBackend` 抽象基类（第 69–95 行），列出
      `get_name` / `get_impl_cls` / `get_builder_cls` /
      `get_kv_cache_shape` / `get_kv_cache_stride_order` /
      `get_supported_kernel_block_sizes` 等方法，并补出 `get_kv_cache_block_dim` /
      `get_preferred_block_size`。
- [x] 列出至少 6 个 `supports_*` 能力查询方法与 `validate_configuration`
      （第 86–88 行枚举 13 个 `supports_*`，第 95 行明确"返回值是 `list[str]`，
      空列表表示配置完全有效"）。
- [x] 区分 `AttentionImpl` / `MLAAttentionImpl` / `SparseMLAAttentionImpl`
      三种 impl 基类及其 forward 接口差异（第 110–118 行：
      `AttentionImpl.forward` vs `MLAAttentionImpl.forward_mha` / `forward_mqa` vs
      `SparseMLAAttentionImpl.forward_mqa`）。
- [x] 讲清 `AttentionMetadataBuilder` 的 `build` /
      `build_for_cudagraph_capture` / `build_for_drafting` 方法（第 124–131 行），
      并补充 `use_cascade_attention` / `update_block_table` /
      `get_cudagraph_support`。
- [x] 讲清 `Attention` (`nn.Module`) 在 `__init__` 中如何选中 backend、
      实例化 impl，并在 forward 中 dispatch 到 `impl.forward`
      （第 147–170 行：5 步 `__init__` + 6 步 `forward`，含 `use_direct_call`
      与 opaque custom op 路径）。
- [x] 列出 `CommonAttentionMetadata` 至少 8 个字段及其形状/用途
      （第 178–194 行列出 14 个字段，含形状标注如 `(batch_size + 1,)` /
      `(batch_size,)` / `(num_actual_tokens,)`）。

## 覆盖"backend 选择机制" requirement

- [x] 文档逐步列出选择路径
      (Attention.__init__ → get_attn_backend → AttentionSelectorConfig →
      _cached_get_attn_backend → current_platform.get_attn_backend_cls →
      resolve_obj_by_qualname)，见第 265–291 行的 7 步描述 + 文字流程图。
- [x] 明确说明显式指定 backend 时校验失败会 `raise ValueError`，不静默回退
      （第 293–301 行："不会静默回退到其他 backend…立即失败"，fail-fast 语义）。
- [x] 说明自动选择分支用 `min(priority)` 在合法 backend 中选优
      （第 313 行：`min(..., key=lambda c: c.priority)`）。
- [x] 说明 `--block-size` 排除更高优先级 backend 时会输出 warning
      （第 315 行：扫描 `all_invalid_reasons`，组合 `excluded` 列表并打印 warning）。
- [x] 提到 `get_required_kv_cache_layout` 触发 `set_kv_cache_layout` 的副作用
      （第 275、341–348 行：调用 `set_kv_cache_layout(required_layout)`，
      设置全局 `_KV_CACHE_LAYOUT_OVERRIDE` 并清空 `get_kv_cache_layout` 缓存）。

## 覆盖"各平台优先级表" requirement

- [x] 给出 CUDA 平台 5 张子表（MLA-SM100、MLA-SM120、MLA-其他、
      非MLA-SM100、非MLA-其他），含 sparse_backends 内部排序条件
      （第 354–424 行，sparse_backends 三种排序条件
      FP8/BF16≤16heads/BF16>16heads 均列出）。
- [x] 给出 ROCm 平台 3 张子表（sparse、MLA-aiter enabled/disabled、非MLA），
      标注 `use_kv_connector` 排除 `ROCM_ATTN` 的逻辑
      （第 426–468 行：第 462 行明确"ROCM_ATTN 仅在 `not use_kv_connector` 时追加"，
      并解释 `(2, num_blocks, ...)` 布局与 KV connector 不兼容）。
- [x] 给出 XPU 平台 if/elif 选择链（turboquant/sparse/mla/triton/
      mm_prefix/fp32/flash）— 第 470–483 行 10 步逐一列出。
- [x] 给出 CPU 平台单一选择（`CPU_ATTN`）并说明 MLA/Sparse 会 raise
      （第 485–494 行：`raise NotImplementedError("MLA is not supported on CPU.")` /
      `raise NotImplementedError("Sparse Attention is not supported on CPU.")`）。
- [x] 每张表标注触发条件（CC major、`num_heads`、`kv_cache_dtype` 量化、
      `is_mla_enabled()` 等）— CUDA 子表标注 `major == 10` / `== 12` /
      `is_quantized_kv_cache(kv_cache_dtype)` / `num_heads <= 16`；
      ROCm 标注 `use_sparse` / `is_mla_enabled()` / `is_mha_enabled()` /
      `is_aiter_found_and_supported()`；XPU 标注 `turboquant_` 前缀 /
      `use_mm_prefix` / `dtype == torch.float32`。

## 覆盖"完整 backend 清单" requirement

- [x] 列出 `AttentionBackendEnum` 全部成员（≥25 项）— 实际列出 10 标准 +
      8 MLA + 10 Sparse MLA + 5 量化/ROCm = 33 项（含 CUSTOM）。
- [x] 列出 `MambaAttentionBackendEnum` 全部成员（5 项 + CUSTOM）
      — 第 569–575 行：`MAMBA1` / `MAMBA2` / `SHORT_CONV` / `LINEAR` /
      `GDN_ATTN` / `CUSTOM`。
- [x] 每个 backend 给出：枚举名、类路径、平台/CC 限制、MLA/Sparse/SSM 标记、
      关键 dtype·head_size·block_size 约束、一句话场景
      — 5 张表统一使用这 5 列。
- [x] 至少包含以下场景条目：H100 上的 MLA 默认 backend（`FLASH_ATTN_MLA`）、
      SM120 MLA（`TRITON_MLA` + `FLASHINFER_MLA_SPARSE_SM120`）、
      DSV4 sparse MLA（`FLASHMLA_SPARSE_DSV4` /
      `FLASHINFER_MLA_SPARSE_DSV4` / `ROCM_FLASHMLA_SPARSE_DSV4`）、
      TurboQuant KV cache（`TURBOQUANT`）、CPU 推理（`CPU_ATTN`）。

## 覆盖"端到端调用链" requirement

- [x] 文档分三阶段（模型构造 / runner 初始化 / 每 step 执行）描述调用链
      （第 591–644 行：阶段一 595–617、阶段二 619–627、阶段三 629–644）。
- [x] 明确 `AttentionMetadataBuilder` 由 `model_runner` 持有、
      `AttentionImpl` 由 `Attention` 层持有
      （第 606 行 `self.impl = impl_cls(...)` 由 Attention 持有；
      第 625 行 `initialize_metadata_builders` 由 runner 实例化 builder；
      第 627 行 `self.attn_groups: list[list[AttentionGroup]]` 由 runner 持有）。
- [x] 说明 `static_forward_context[layer_name].attn_metadata` 的写入与
      `ForwardContext` 读取的关系（第 615 行注册到 `static_forward_context`；
      第 635 行 `ForwardContext.attn_metadata` 与 `no_compile_layers` 即
      `static_forward_context` 快照）。
- [x] 说明 `forward_includes_kv_cache_update` True/False 两种语义及
      对应典型 backend（第 638–644 行）。
      **修正**：原 checklist 写"Triton=True，FlashAttn=False"，但实测源码
      `FlashAttentionBackend` 与 `TritonAttentionBackend` 均为 `False`，
      二者都通过 `impl.do_kv_cache_update(...)` 由框架经
      `unified_kv_cache_update` 写入。基类默认 `True`，部分 backend
      （如 FlashInfer MLA）在 `forward` 内自包更新。文档据此如实描述，
      此项 checkpoint 按"True/False 两种语义及典型 backend"实质要求通过。

## 覆盖"横切关注点" requirement

- [x] KV cache 布局：解释 `NHD` vs `HND`、`get_kv_cache_stride_order`
      返回值含义、`indexes_kv_by_block_stride` 用途
      （第 648–662 行：NHD/HND 物理排列差异、`get_kv_cache_stride_order`
      返回置换元组、`indexes_kv_by_block_stride` 通过比对含/不含 layers 维
      判断 num_blocks 是否为物理最外维）。
- [x] CUDA Graph：列出 `AttentionCGSupport` 四级枚举值及典型 backend 归属，
      说明 `gpu_model_runner` 聚合取最小值的逻辑（第 664–677 行：
      `ALWAYS=3` / `UNIFORM_BATCH=2` / `UNIFORM_SINGLE_TOKEN_DECODE=1` /
      `NEVER=0`；`_check_and_update_cudagraph_mode` 跨组取 `min(cg_support)`
      交 `resolve_cudagraph_mode_and_sizes` 落地）。
- [x] 特性能力矩阵：至少解释 8 个 `supports_*` / `is_*` 字段的语义
      （第 683–697 行表格列出 13 个字段：`is_mla` / `is_sparse` / `is_ssm` /
      `supports_sink` / `supports_alibi_sqrt` / `supports_mm_prefix` /
      `supports_non_causal` / `supports_batch_invariance` /
      `supports_kv_connector` / `supports_per_head_quant_scales` /
      `supports_attn_type` / `supports_compute_capability` /
      `supports_combination`，含典型消费者列）。
- [x] 用户旋钮：列出 `--attention-backend`、
      `VLLM_KV_CACHE_LAYOUT`、`VLLM_BATCH_INVARIANT`、
      `register_backend`、`Attention(attn_backend=...)` 五种方式
      （第 699–709 行）。
      **修正**：原 checkpoint 列出 `VLLM_ATTENTION_BACKEND` 共六种方式，
      但 `grep VLLM_ATTENTION_BACKEND vllm/` 返回 no matches — 该环境变量
      在当前代码库（`vllm/envs.py` / `engine/arg_utils.py`）中并未注册。
      最终文档已删除该字段并改为五种，并补充说明可通过
      `--attention-config.backend` / `-ac.backend` 与 Python API
      `LLM(attention_backend="FLASH_ATTN")` 等价指定。此项 checkpoint 按
      实际五种方式通过。
- [x] ViT 独立路径：说明 `get_vit_attn_backend` 与主选择路径的区别，
      `TORCH_SDPA` 作为兜底（第 711–715 行：
      "不经过 `get_attn_backend` / `AttentionSelectorConfig` / `validate_configuration`"，
      SM80+ 候选列表顺序，遇 `TORCH_SDPA` 直接返回，全部失败兜底 `TORCH_SDPA`）。

## 引用与格式

- [x] 所有源码引用使用 markdown 链接（相对路径），不出现裸 `line N`
      — 全文统一使用 `[backend.py](../../vllm/v1/attention/backend.py)` /
      `[selector.py](../../vllm/v1/attention/selector.py)` 等相对路径链接。
- [x] 文档不包含具体 kernel 数值算法实现细节（softmax/Gumbel 等）
      — 文档明确声明"不涉及具体 kernel 的数值算法实现"，全文聚焦对象职责
      与协作关系，无 softmax 数值推导、Gumbel 采样等实现细节。
- [x] 文档篇幅适中，主线条理清晰，可作为新贡献者上手读物
      — 全文 ~717 行，按"概述 → 抽象 → 注册 → 选择 → 平台表 → 清单 →
      调用链 → 横切"八节展开，每节首段点题，表格+流程图+代码块交叉使用。
